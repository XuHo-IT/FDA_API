# FE-12, FE-13, FE-14 - Alert & Notification System

## Tổng kết sau khi review code

### ✅ Đã có sẵn (không cần code thêm):

| Component | Status | Location |
|-----------|--------|----------|
| AlertProcessingJob | ✅ | BackgroundJobs/Feat42_ProcessAlerts |
| NotificationDispatchJob | ✅ | BackgroundJobs/Feat43_DispatchNotifications |
| PushNotificationService | ✅ Enabled | Notifications/ |
| EmailService | ✅ Exists | Notifications/ |
| SmsService | ✅ Exists | Notifications/ |
| PriorityRoutingService | ✅ Exists | Notifications/ |
| User.FcmToken storage | ✅ | LoginHandler lưu khi login |
| FcmToken usage | ✅ | DispatchNotificationsHandler: `user.FcmToken` |
| Severity threshold | ✅ | UserAlertSubscription.MinSeverity (FE-14 đã có!) |
| Retry logic | ✅ | DispatchNotificationsHandler lines 259-306 |
| Quiet hours | ✅ | DispatchNotificationsHandler lines 161-165 |

---

## FE-12: Receive Flood Alerts ✅ ĐÃ HOÀN THÀNH VỀ CODE

**Kết luận**: Code đã sẵn sàng. Cần verify bằng cách:
1. Chạy backend
2. Tạo test alert (giả lập sensor reading vượt threshold)
3. Kiểm tra NotificationLog được tạo

---

## FE-13: Priority Flood Notifications

### Cần điều chỉnh:
- **Current**: Free users delay 60-120 seconds
- **Requested**: Free users delay 5 minutes (300 seconds)

### Files to Modify:
- `PriorityRoutingService.cs` - Update `GetDispatchDelaySeconds`

```csharp
// Current (line 87-88):
return priority >= NotificationPriority.High ? 60 : 120;

// Change to:
return priority >= NotificationPriority.High ? 300 : 300; // 5 minutes for all
```

---

## FE-14: Customize Alert Thresholds ✅ ĐÃ HOÀN THÀNH VỀ CODE

**Kết luận**: Tính năng đã có sẵn!

- `UserAlertSubscription.MinSeverity` - User set mức cảnh báo tối thiểu (info, caution, warning, critical)
- Handler kiểm tra: DispatchNotificationsHandler lines 152-158

**Ví dụ**:
- User set `MinSeverity = "warning"` → Chỉ nhận notification khi alert severity = warning hoặc critical
- Không nhận info, caution

---

## Tóm tắt:

| Task | Status | Action |
|------|--------|--------|
| FE-12 | ✅ Code sẵn sàng | Test end-to-end |
| FE-13 | ⚠️ Cần sửa delay | 60s → 300s (5 min) |
| FE-14 | ✅ Đã có sẵn | Verify API |

---

## Plan chi tiết:

### Task 1: FE-13 - Sửa delay time
- [ ] Modify `PriorityRoutingService.cs` - Change delay from 60-120s to 300s (5 min) for free users

### Task 2: FE-12 - Verify end-to-end (Optional - could be manual test)
- [ ] Backend team test with sample alert

### Task 3: FE-14 - Verify existing API
- [ ] Check if UserAlertSubscription API exists for setting MinSeverity
