# .env
POSTGRES_DB=FDA
POSTGRES_USER=user
POSTGRES_PASSWORD=hoa09062004
CONNECTION_STRING_SERVER=postgres # Use 'postgres' (service name) or 'host.docker.internal' (host IP)

# Define the final connection string key your application expects
ConnectionStrings__PostgreSQLConnection=Server=${CONNECTION_STRING_SERVER};Port=5432;Database=${POSTGRES_DB};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD};